#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * by kilimini
 *
 * Return: the absolute value of int
 */

int _abs(int a)
{
	if (a < 0)
		return (-a);
	return (a);
}
